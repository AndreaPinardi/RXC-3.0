import React, { FC } from "react";

interface RXC_BRAND_FOOTER_TYPE {
  position: string;
}

const RXC_BRAND_FOOTER: FC<RXC_BRAND_FOOTER_TYPE> = (props) => {
  return <div className="absolute bottom-2 left-2">RXC_BRAND_FOOTER</div>;
};

export default RXC_BRAND_FOOTER;
