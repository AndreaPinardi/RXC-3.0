import React, { FC } from "react";

interface RXC_FRAME_IMAGE_TYPE {
}

const RXC_FRAME_IMAGE: FC<RXC_FRAME_IMAGE_TYPE> = (props) => {
  return <div>RXC_FRAME_IMAGE</div>;
};

export default RXC_FRAME_IMAGE;